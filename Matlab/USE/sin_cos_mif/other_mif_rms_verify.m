% 离散正弦波参数
A = 3; % 振幅
f = 50; % 频率
phi = pi/4; % 相位

% 采样频率和时间长度
fs = 1000; % 采样频率为1000Hz
t =0:1/fs:1; % 时间长度为1秒

% 生成离散正弦波信号
x = A*sin(2*pi*f*t + phi);

% 计算离散正弦波信号的均方根值
rms_x = rms(x);

% 输出结果
fprintf('离散正弦波信号的有效值为：%.4f\n', rms_x);
